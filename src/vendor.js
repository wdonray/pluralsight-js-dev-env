/* eslint-disable no-unused-vars, import/default */

/*
  This file contains references to the third party libraries that we are using in this project.
  This is used by webpack in the production build only.
  A separate bundle is useful since it's unlikely to change as often as the application's code.
  Caches them to avoid downloading huge JS files anytime a line of code changes.
*/


import fetch from 'whatwg-fetch'
